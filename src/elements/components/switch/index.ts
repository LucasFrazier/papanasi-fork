import Root from './root.lite';
import Trigger from './trigger.lite';
import Label from './label.lite';
import OnLabel from './onLabel.lite';
import OffLabel from './offLabel.lite';

const Switch = {
  Root: Root,
  Trigger: Trigger,
  Label: Label,
  OnLabel: OnLabel,
  OffLabel: OffLabel
};

export default Switch;
