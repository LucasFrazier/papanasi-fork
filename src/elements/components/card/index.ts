import Root from './root.lite';
import Media from './media.lite';
import Title from './title.lite';
import Description from './description.lite';
import CTA from './cta.lite';
import ActionBar from './actionBar.lite';

const Card = {
  Root: Root,
  Media: Media,
  Title: Title,
  Description: Description,
  CTA: CTA,
  ActionBar: ActionBar
};

export default Card;
