import End from './end.lite';
import Middle from './middle.lite';
import Root from './root.lite';
import Start from './start.lite';

const Footer = {
  Root: Root,
  Start: Start,
  Middle: Middle,
  End: End
};

export default Footer;
